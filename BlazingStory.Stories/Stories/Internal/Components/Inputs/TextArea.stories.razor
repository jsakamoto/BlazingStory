@attribute [Stories("Internals/Components/Inputs/TextArea")]

<Stories TComponent="TextArea" Layout="typeof(MarginedFrame)">

    <Story Name="Default">

        <Arguments>
        </Arguments>

        <Template>
            <TextArea @attributes="context.Args">
            </TextArea>
        </Template>

    </Story>

</Stories>
