{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "group": {
                "kind": "build",
                "isDefault": true,
            },
            "label": "build package",
            "type": "shell",
            "command": "dotnet",
            "args": ["pack", "BlazingStory.ProjectTemplates.msbuild"],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}