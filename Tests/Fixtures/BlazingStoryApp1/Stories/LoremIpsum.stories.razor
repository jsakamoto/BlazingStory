@attribute [Stories("Lorem/Ipsum/Header")]

<Stories TComponent="Button">

    <Story Name="Docs">
        <Template>
            <Button Text="Docs" Color="ButtonColor.Primary" @attributes="context.Args" />
        </Template>
    </Story>

    <Story Name="Logged In">
        <Template>
            <Button Text="Logged In" Color="ButtonColor.Primary" @attributes="context.Args" />
        </Template>
    </Story>

    <Story Name="Logged Out">
        <Template>
            <Button Text="Logged Out" Color="ButtonColor.Primary" @attributes="context.Args" />
        </Template>
    </Story>

</Stories>