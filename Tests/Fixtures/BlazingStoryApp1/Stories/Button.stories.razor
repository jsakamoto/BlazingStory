@attribute [Stories("Examples/Button")]

<Story TComponent="Button" Name="Default">
    <Template>
        <Button Color="ButtonColor.Default" @attributes="context.Args" />
    </Template>
</Story>

<Story TComponent="Button" Name="Primary">
    <Template>
        <Button Color="ButtonColor.Primary" @attributes="context.Args" />
    </Template>
</Story>
