@attribute [Stories("Components/atoms/IconButton")]

<Stories TComponent="IconButton">

    <ArgType For="_ => _.Icon" Control="ControlType.Select" />
    <ArgType For="_ => _.Type" Control="ControlType.Select" />
    <ArgType For="_ => _.Color" Control="ControlType.Color" />

    <Story Name="Default">
        <!--
        Add a description of this story in the <Description> render fragment like this:
        -->
        <Description>
            <b>Description:</b>
            <span>
                This section describes the usual usage of the <code>&lt;IconButton&gt;</code> component.
            </span>
        </Description>

        <Arguments>
            <Arg For="_ => _.Icon" Value="Icon.Add" />
            <Arg For="_ => _.Type" Value="ButtonType.Button" />
        </Arguments>

        <Template>
            <IconButton @attributes="context.Args">
            </IconButton>
        </Template>

    </Story>

</Stories>
